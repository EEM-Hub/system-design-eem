---
source: sources/wiki-ETag.md
source_url: https://en.wikipedia.org/wiki/ETag
---

## ETag — Disambiguation of Terms

This is a disambiguation page, not a substantive technical document. It lists multiple unrelated uses of the term "Etag" across different domains. For system design purposes, the only relevant entry is **HTTP ETag**.

## Key Concepts

- **HTTP ETag (Entity Tag)**: Part of the HTTP protocol; used for web cache validation and conditional requests. This is the meaning relevant to system design.
- The term "Etag" is overloaded across unrelated domains (text editors, electricity markets, tolling systems, EU technical standards).

## Commands and Syntax

None provided — this is a disambiguation page with no technical content.

## Relationships

- **HTTP ETag** connects to: HTTP caching, conditional GET requests (`If-None-Match`), cache invalidation, CDN behavior, RESTful API design, optimistic concurrency control.
- The other meanings (ctags/Emacs, electricity trading, tolling, EU standards) are irrelevant to system design.

## Exam-Relevant Points

- If an exam question references "ETag," it almost certainly means the **HTTP ETag** header unless explicitly stated otherwise.
- Know that ETag is an HTTP response header used for cache validation — the actual technical details would be on the HTTP ETag page, not here.

---

**Note:** This page has no real technical depth. The HTTP ETag article itself would be the appropriate source for system design study material.
